{
  "name": "boilerspace",
  "private": true,
  "scripts": {
    "start": "bash -c 'source ~/.nvm/nvm.sh 2>/dev/null; nvm use 2>/dev/null; exec concurrently -n backend,client -c blue,magenta \"npm run start:backend\" \"npm run start:client\"'",
    "start:backend": "cd backend && npm start",
    "start:client": "cd client && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
